<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-secondary">
    <%= link_to "リハコネ", root_path, class: "navbar-brand" %>
      <% if client_signed_in? %>
        <h6 class="text-light">ようこそ、<%= current_client.last_name %>さん！</h6>
      <% end %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
          <% if client_signed_in? %>
            <li class="nav-item btn">
              <%= link_to "マイページ", clients_my_page_path, class: 'nav-link text-light' %>
            </li>
            <li class="nav-item btn">
              <%= link_to "記録一覧", client_records_path, class: 'nav-link text-light' %>
            </li>
            <li class="nav-item btn">
              <%= link_to "ログアウト", destroy_client_session_path, method: :delete, class: 'nav-link text-light' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>